{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug goMH",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "console": "integratedTerminal",
            "args": [
                // Сюда можно передать аргументы. Например:
                // "-config=http://example.com/config.json"
            ]
        }
    ]
}
